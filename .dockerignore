# Docker build context exclusions
# Reduces build time and image size

# Training results and checkpoints (large, frequently changing)
MOC_Training/training_results/
MOC_Training/models/
MOC_Training/__pycache__/
MOC_Training/*.pyc

# UE5 build artifacts (huge, not needed in container)
Binaries/
Intermediate/
Saved/
DerivedDataCache/
Build/

# Git and IDE
.git/
.gitignore
.vscode/
.vs/
*.sln
*.vcxproj

# Windows system files
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Documentation (not needed in runtime image)
*.md
!MOC_Training/README.md

# Plugins (except Schola)
Plugins/*
!Plugins/Schola-1.3.0/

# Content (not needed for training)
Content/

# Source code (only needed during development)
Source/

# Keep only what's needed:
# - MOC_Training/ (Python scripts)
# - Plugins/Schola-1.3.0/Resources/python (Schola package)
